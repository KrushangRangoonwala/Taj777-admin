copy below code's exact same html struture with exact same css classes and exactly same attributes in this react project
if u find any library in below html code, then use that library in this react project

if u find same structure repeating multiple time then use .map in react, or just made re-usable components
also maintain code readability and maintainability, so dont make .map if it is not necessary


--------------------

copy below code's exact same html struture with exact same css classes and exactly same attributes in this react project
if u find any library in below html code, then use that library in this react project

-------------

instructions :
copy below html file's code's exact same html struture with exact same css classes and exactly same attributes in this react project
if u find any library in below html code, then use that library in this react project

if u find same structure repeating multiple time then use .map in react, or just made re-usable components
also maintain code readability and maintainability, so dont make .map if it is not necessary


task :
org_html = file_name.jsx
admin_game_file = file_name.jsx
old_game_file = file_name.jsx

copy this <org_html> code structure in  <admin_game_file>

for <div className="casino-video"> element of <org_html> use casinovideo.jsx
for <div class="right-sidebar"> element of <org_html> use casinorightsidebar.jsx

take reffrence of html structure from <org_html> file and take refference of logics and how to set socket data from <old_game_file> file

take refference for setting up socket connection from @OneCard1day.jsx file

comment out exposure logic from <admin_game_file> as done in @OneCard1day.jsx file