

copy html code's exact same html struture with exact same css classes and exactly same attributes in this react project
if u find any library in html code, then use that library in this react project

if u find same structure repeating multiple time then use .map in react, or just made re-usable components
also maintain code readability and maintainability, so dont make .map if it is not necessary

html code = @qqq
copy this in 